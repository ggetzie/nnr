[pytest]
addopts = --ds=config.settings.test
# pytest's default is test_*.py, which silently skips the tests.py files that
# Django's own layout (and this project) uses.
python_files = tests.py test_*.py *_tests.py
